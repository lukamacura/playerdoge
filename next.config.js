/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['firebasestorage.googleapis.com', 'cdn.playerdoge.com'],
  }
}

module.exports = nextConfig
